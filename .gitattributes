# Normalize line endings to LF in the repo and on checkout, regardless of the
# committer's/checkout OS. Prevents deploy/* shell scripts from picking up
# CRLF endings, which breaks them on the Linux/FreeBSD server.
* text=auto eol=lf
